# Credentials -------------------------------------------------------------
mongo_credentials <- config::get(file = "conf/globalresources.yml")
# Setup -------------------------------------------------------------------
rm(list = ls())
gc()
# Packages ----------------------------------------------------------------
if(!require('dplyr')){install.packages('dplyr')}
if(!require('mongolite')){install.packages('mongolite')}
# Credentials -------------------------------------------------------------
mongo_credentials <- config::get(file = "conf/globalresources.yml")
# Credentials -------------------------------------------------------------
mongo_credentials <- config::get(file = "config/globalresource.yml")
mongo_credentials
# Credentials -------------------------------------------------------------
mongo_credentials <- config::get(file = "config/globalresource.yml")
mongo_credentials
# All collections
mongo(url = mongo_credentials$mongoURL)$run('{"listCollections":1}')$cursor$firstBatch %>% as_tibble()
# Metadata on a collection
db = mongo(collection = "collection1", url = mongo_credentials$mongoURL)
db
# Credentials -------------------------------------------------------------
mongo_credentials <- config::get(file = "config/globalresource.yml")
# All collections
mongo(url = mongo_credentials$mongoURL)$run('{"listCollections":1}')$cursor$firstBatch %>% as_tibble()
# Credentials -------------------------------------------------------------
mongo_credentials <- config::get(file = "config/globalresource.yml")
mongo_credentials
# All collections
mongo(url = mongo_credentials$mongoURL)$run('{"listCollections":1}')$cursor$firstBatch %>% as_tibble()
mongo_credentials$mongoURL
tabela_final_v30 <- mongo("tabela_teste", url = mongo_credentials$mongoURL)
# Setup -------------------------------------------------------------------
rm(list = ls())
gc()
# Packages ----------------------------------------------------------------
if(!require('dplyr')){install.packages('dplyr')}
if(!require('mongolite')){install.packages('mongolite')}
# Credentials -------------------------------------------------------------
mongo_credentials <- config::get(file = "config/globalresource.yml")
mongo_credentials
# All collections
mongo(url = mongo_credentials$mongoURL)$run('{"listCollections":1}')$cursor$firstBatch %>% as_tibble()
# Setup -------------------------------------------------------------------
rm(list = ls())
gc()
# Packages ----------------------------------------------------------------
if(!require('dplyr')){install.packages('dplyr')}
if(!require('mongolite')){install.packages('mongolite')}
# Credentials -------------------------------------------------------------
mongo_credentials <- config::get(file = "config/globalresource.yml")
tabela_final_v30 <- mongo("tabela_teste", url = mongo_credentials$mongoURL)
mongo_credentials
tabela_final_v30 <- mongo("collection_teste", url = mongo_credentials$mongoURL)
